name: SonarQube Analysis

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  pull-requests: read  # Required for PR decoration

jobs:
  sonarqube:
    runs-on: ubuntu-latest
    env:
      # If you have private Gems from GitHub Packages 
      # (remove if not needed)
      BUNDLE_RUBYGEMS__PKG__GITHUB__COM: "y2kstack:${{ secrets.MY_PAT }}"

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      # Setup Java 17 for your project build (the scan action itself also runs Java 17 internally)
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # Install dependencies if needed (Ruby, Node, Maven, etc.)
      # Example for Ruby (remove if not needed):
      # - name: Set up Ruby
      #   uses: ruby/setup-ruby@v1
      #   with:
      #     ruby-version: '3.0'
      #     bundler-cache: true
      # - run: bundle install

      - name: SonarQube Scan
        # You can pin the exact commit or the version.
        # uses: SonarSource/sonarqube-scan-action@v1.1.0
        uses: SonarSource/sonarqube-scan-action@7295e71c9583053f5bf40e9d4068a0c974603ec8
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}   # For PR info
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}     # Token from SonarQube
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }} # Your Nginx public URL
        with:
          args: >
            -Dsonar.projectKey=git-test-jp
            -Dsonar.sources=.
            -Dsonar.java.binaries=.
